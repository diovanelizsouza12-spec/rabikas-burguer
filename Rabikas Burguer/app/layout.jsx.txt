export const metadata = {
  title: "Rabika's Burguer",
  description: "Cardápio Digital + Pedidos no WhatsApp",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>
        {children}
      </body>
    </html>
  );
}